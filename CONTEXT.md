# Cas Personal Agent

单用户、私有部署的个人工作管理 Agent。用户在聊天入口（飞书 Bot，后续微信）里用自然语言倾倒工作安排、进展、等待、想法；Agent 负责拆分、维护当前状态、恢复上下文、压缩用户面对的选择。存在的理由：用户脑内内容多、注意力易岔开，需要一个降低决策成本的助手，而不是一个记录更多信息的工具。

## Language

### 入口与回合

**入口（Channel）**:
用户与 Agent 对话的聊天软件适配层。MVP 只有飞书 Bot；微信 Bot 是后续第二个入口。
_Avoid_: 前端、客户端、dashboard

**原始事件（Event）**:
一次外部输入或系统处理结果的追加式记录，包含用户原话、Agent 回复、工具调用和执行结果。永不被摘要覆盖。
_Avoid_: 聊天记录、消息日志、历史

**逻辑对话（Logical Conversation）**:
用户视角里唯一且永不结束的那条对话。底层可以由多个 Pi session 轮换承载。
_Avoid_: 会话、聊天、thread

**Pi session**:
Pi 运行时的一段物理工作记忆，可退休、可被新 session 替换。它不是用户模型、状态或历史的载体。
_Avoid_: 对话、context、会话（不加"Pi"前缀时）

**交接卡（Handoff）**:
旧 Pi session 退休时生成的结构化恢复信息：当前 focus、已确认事实、未闭环问题、等待与预案、近期决策、不应重复的路径、下一次进入点。只含恢复工作所需，不是全文摘要。
_Avoid_: 摘要、总结、compaction

**轮换（Rollover）**:
用完整回合后退休当前 Pi session、用交接卡启动新 session 的过程。对用户无感。
_Avoid_: 重启、重置、新开对话

### 当前状态

**项目（Project）**:
用户在跟的一条业务线或持续性事务，有目标、阶段、状态和当前摘要。事项挂在项目下；无明确项目的事项可以不挂。
_Avoid_: 业务、任务组、工程

**事项（Item）**:
从用户原话中提炼出的单个可识别对象。有正交的两个维度：**类型**（任务、想法、问题、决策、信息）和**状态**（收件箱、可行动、进行中、等待、已排期、完成、放弃、归档）。
_Avoid_: 任务（作为总称时）、待办、todo、条目

**想法（Idea）**:
类型为"想法"的事项。"我在想、也许、有没有可能"默认落入此类。想到不等于承诺要做；想法默认不进入当前注意力。
_Avoid_: 待办、候选任务、backlog

**下一步（Next Action）**:
一个 active 事项上唯一的、可直接开始的动作。一个事项同一时刻只有一个。
_Avoid_: 子任务、步骤列表、计划

**等待（Waiting）**:
事项状态之一：当前不可行动，因为在等人、结果、材料或时间。必须记录等谁/等什么、从何时开始、解除条件、何时重新检查。
_Avoid_: 阻塞、挂起、pending

**条件预案（Contingency）**:
挂在事项上的"若 X 则 Y"分支，例如"若周五无回复则联系张总"。预案在触发前不是行动，不进入当前注意力。
_Avoid_: 备选任务、计划 B、后续步骤

**检查点（Checkpoint）**:
事项（尤其是等待或暂存内容）何时重新进入注意力的时间点。它不是任务的 deadline，也不是会议时间；到点触发一次到期提醒。
_Avoid_: 时间、deadline、到期日、review 时间

**行动（Action）**:
从事项中分离出的、可以执行完成或必须在特定时间发生的明确对象。MVP 分为个人行动（滴答）、协同承诺（飞书任务）和时间安排（暂由 Bitable 持有）；企微日程在 P1 接管时间安排的执行事实。行动有 deadline 或起止时间；事项只有检查点。
_Avoid_: 任务（作为总称时）、外部任务、同步项

**时间安排（Scheduled Event）**:
必须在明确起止时间发生的会议、预约或日程。MVP 将它挂在 Bitable 的事项下并触发一次提醒，但不把开始时间伪装成事项检查点。
_Avoid_: 检查点、deadline、待办

**当前注意力（Focus）**:
用户此刻应该面对的 1–3 个可行动事项，由确定性规则从当前状态计算得出，LLM 只负责解释和压缩。不是用户手动标的固定优先级。
_Avoid_: 优先级、今日待办、P0

**稍后区（Parking Lot）**:
想法、未激活候选和暂时挂起内容所在的视图。它们保留但不占用注意力。
_Avoid_: 归档、垃圾桶、backlog

**认知模式（Cognitive Mode）**:
用户此刻的工作方式：explore（发散、允许关联）或 execute（收束、维持焦点）。决定 Agent 岔开时是否拉回。MVP 只区分这两态加 unknown。
_Avoid_: 心情、状态（会与事项状态混淆）

### 记忆

**长期记忆（Memory）**:
Hindsight 中可跨 session 召回的事实、经历、观察和对用户工作方式的理解。回答"过去知道和经历过什么"，不回答"现在是什么状态"。
_Avoid_: 知识库、历史、上下文

**当前状态（Current State）**:
SQLite 中可直接查询、可重启恢复的项目、事项与行动挂载当前读模型。飞书多维表格是可编辑展示面；聊天指令、展示面人工纠正和外部执行结果都经版本化状态更新进入本地。省略字段保留原值，明确清空才删除；较旧的操作或外部快照不能覆盖较新的明确纠正。当记忆或 Agent 推断与用户最近确认的状态冲突时，以后者为准。
_Avoid_: 历史、长期记忆、聊天摘要

**展示面（State View）**:
用户浏览和校正当前状态的可编辑视图。首版使用飞书多维表格，不要求另做 dashboard。
_Avoid_: 输入入口、长期记忆、原始事件库

**权威纠正（Authoritative Correction）**:
用户在多维表格、滴答清单、飞书任务或企业微信日程中的明确手动修改。它优先于冲突的 Agent 推断、旧摘要和旧记忆。
_Avoid_: 手工例外、临时覆盖

**事实源（Fact Owner）**:
某一类执行事实当前值的唯一权威系统。Bitable 拥有项目、事项以及 MVP 的时间安排；滴答、飞书任务拥有其原生执行对象的事实，P1 接入企微日程后由企微接管公司日程事实。SQLite Current State 是统一本地读模型与请求/确认状态，不因保存副本而接管执行事实。
_Avoid_: 同步目标、主数据库

**行动挂载（Action Link）**:
把外部任务或日程关联到解释其存在原因的项目和事项。它保存关系而不复制事实源的所有字段。
_Avoid_: 任务副本、重复记录

**待核对队列（Verification Queue）**:
SQLite 中可重启恢复的外部事实定向核验请求。来源包括 CAS 写出/失败、真实外部事件、范围快照缺失和用户显式刷新；它受连接器预算、超时、退避与不重叠约束。范围快照缺失只产生 unknown，不等价于完成、删除或移动。
_Avoid_: change feed、全量同步队列

**到期提醒（Due Reminder）**:
仅由用户明确说出的时间安排、deadline 或复查节点触发的一次主动通知。首版不自行猜测提前量，也不发送固定频率的通用汇总。
_Avoid_: 每日推送、例行打扰

**保留（Retain）**:
把一条经过筛选的信息写入长期记忆。异步执行，失败不阻断回复。
_Avoid_: 保存、记录（这两个词留给原始事件）

**召回（Recall）**:
每回合前由 Supervisor 用当前消息、项目/事项名和认知模式向长期记忆发起的检索。结果限量、标来源、可降级跳过。
_Avoid_: 搜索、查询、RAG

**工作模型（Working Model）**:
Agent 对用户行为模式的判断，例如"用户在什么条件下容易岔开"。是带证据、置信度、适用范围、可撤回的假设，不是人格标签。MVP 只积累证据不生成。
_Avoid_: 用户画像、性格、习惯
